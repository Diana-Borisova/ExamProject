spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: 1234
    url: jdbc:mysql://localhost:3306/food_planner?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
      enabled: true
  mvc:
    hiddenmethod:
      filter:
        enabled: true

  cloudinary:

  cloud_name: "dmd3mttpc"
  api_key: "799357567129288"
  api_secret: "-0ZR-Nt4ESQJ_BVBqAzyryTUAO0"

management:
  server:
    port: 8080
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"

        spring:
          mvc:
            hiddenmethod:
              filter:
                enabled=true:
